---

# ⭐ **PROJECT NAME**

**AI-Powered Financial Robo-Advisor with ML-Based Risk Profiling + MPT Portfolio Optimizer**

---

# 🚀 TECH STACK (Lightweight)

### Backend (ML + API)

* **Python + FastAPI** (extremely lightweight, <50MB memory usage)
* **Scikit-learn** (for RandomForest)
* **Pandas, NumPy** (basic ops)
* **PyPortfolioOpt** (for MPT)

### Frontend

* **HTML + CSS + vanilla JS** (no React needed)
* Simple chat-style UI

### Data

* Synthetic CSV dataset (generated in notebook)

---

# 🧱 PROJECT CORE MODULES (minimum required)

We only need **4 components** to make this fully working:

---

## **1️⃣ Synthetic Data Generator**

Already defined in your paper.
Implement a tiny Python script to generate ~2000 rows.

Features:

* age
* income
* investment_experience
* risk_tolerance_score
* stress_tolerance
* confidence_level
* loss_aversion
* financial_goal_horizon
* label (Conservative / Moderate / Aggressive)


---

## **2️⃣ ML Model (Random Forest Classifier)**

Train a simple RandomForest:

```
model = RandomForestClassifier(n_estimators=200)
model.fit(X_train, y_train)
```

Save model as:

```
model.pkl
```

Include feature importance (optional).

---

## **3️⃣ Portfolio Optimizer using MPT**

Use **PyPortfolioOpt**:

Steps:

1. Load mock historical data (5–10 assets)
2. Compute returns + covariance
3. Optimize for each risk level:

   * Conservative → minimum volatility
   * Moderate → efficient risk-return balance
   * Aggressive → max Sharpe ratio

Return weights like:

```
{
 "AAPL": 0.25,
 "MSFT": 0.30,
 "GOOG": 0.20,
 "BND": 0.25
}
```

**Time required: 45 mins**

---

## **4️⃣ Conversational UI (super lightweight)**

HTML page with:

* A chat text box
* JavaScript that sends user messages to FastAPI
* Backend uses simple rule-based NLP (you DON’T need full NLP)

Example workflow:

User: *“I want high returns but I can't take big losses”*
→ Extract keywords → Map to behaviour → Use ML to classify → Return risk class
→ Generate portfolio → Display results

**Time required: 1.5 hours**

---

# 🧩 FINAL SYSTEM ARCHITECTURE (simple + aligned with paper)

This matches your 6-layer enterprise architecture **but simplified**:

**Frontend (Chat UI)**
→ **FastAPI** (integration layer)
→ **Risk Classifier (ML layer)**
→ **MPT Portfolio Engine**
→ **Return portfolio + explanations**

That's it.

---

# 🗂️ Folder Structure (copy this directly)

```
project/
│── backend/
│   ├── main.py            # FastAPI
│   ├── model.pkl          # trained RF model
│   ├── optimizer.py       # MPT logic
│   ├── preprocess.py
│   └── synthetic_data.py
│
│── frontend/
│   ├── index.html
│   ├── style.css
│   └── script.js
│
│── notebooks/
│   └── Capstone.ipynb     # your original notebook
│
│── README.md
```

---

# 🕒 TIME BREAKDOWN (6 hours)

| Task                      | Time   |
| ------------------------- | ------ |
| Setup + synthetic dataset | 30 min |
| Train RandomForest        | 40 min |
| Build MPT optimizer       | 45 min |
| FastAPI backend           | 45 min |
| HTML/JS chat UI           | 90 min |
| Testing + reports         | 30 min |
| Final polish              | 20 min |

You will finish in **~5h 20m**.

---

# 📌 NEXT STEP

If you want, I can now generate:

### ✅ All backend code (FastAPI + ML + MPT)

### ✅ All UI code (HTML/CSS/JS)

### ✅ The synthetic dataset generator

### ✅ The README for submission

### ✅ The final architecture diagram

### ✅ A 2-min demo script for presentation

Just say **“generate the full code”** or **“start with backend”**.


Absolutely YES — you can use **SQLAlchemy** for the DB, and you *should* if you want user accounts + login + returning users.
And YES — I’ll make you a **fully working AI chatbot** (not a fake rule-based bot).

Below is the **final, clean architecture** using:

* **FastAPI**
* **SQLAlchemy + SQLite (lightweight)**
* **JWT for login security**
* **Random Forest ML model**
* **MPT optimizer**
* **Conversational AI engine (real NLP + intent parsing)**
* **Frontend HTML/CSS/JS chat UI**

Once you confirm, I will generate **ALL the code files** (copy-paste ready).

---

# ✅ FINAL ARCHITECTURE WITH USER LOGIN + CHATBOT + ML + MPT

```
frontend/
   index.html
   script.js
   style.css

backend/
   main.py            # FastAPI API
   auth.py            # JWT login/signup
   database.py        # SQLAlchemy models + session
   models.py          # User model
   chatbot.py         # NLP & conversation manager
   ml_model.pkl       # Random Forest model
   portfolio.py       # MPT engine
   risk_classifier.py # risk classification pipeline
   requirements.txt

database/
   roboadvisor.db     # SQLite DB auto created
```

---

# 🎯 WHAT THE SYSTEM WILL DO

### 1️⃣ **User Signup/Login (SQLAlchemy + JWT)**

* User signs up with email + password (hashed)
* Login returns a JWT token
* Token is required to access the chatbot API
* Database stored on **SQLite** for simplicity

### 2️⃣ **Conversational AI Chatbot**

The chatbot can:

✔ Understand user intent (risk questions / preferences)
✔ Extract behavioral inputs
✔ Maintain conversation context
✔ Ask missing questions
✔ Predict the user's **risk profile** using your Random Forest model
✔ Give an **MPT-optimized portfolio**

### 3️⃣ **ML + MPT Integration**

* Classifier → *Conservative / Moderate / Aggressive*
* MPT returns optimized weights for each class

---

# 🚀 NEXT STEP: I GENERATE ALL CODE FOR YOU

Before I generate the entire working application, I need **ONE confirmation**:

## ❓ Which AI engine for the chatbot do you want?

### **Option A — Lightweight NLP Engine (Recommended for hackathon)**

* Keyword + pattern + intent model
* Super fast
* Zero GPU
* Works offline
* Enough to simulate real AI-like conversations

### **Option B — Local ML-powered NLP**

* Uses fastText or small transformers
* Slower but smarter
* Might increase system load

### **Option C — Connect to OpenAI/ChatGPT API**

* Best quality
* Requires Internet
* Zero local load
* Easiest to get "real AI"

---

# 👉 Reply with **A, B, or C**

And then I will immediately generate:

### ✅ complete backend code

### ✅ SQLAlchemy setup

### ✅ JWT auth

### ✅ Chatbot engine

### ✅ Risk classifier integration

### ✅ MPT optimizer

### ✅ Full frontend chat UI

### ✅ Step-by-step run guide

Just send: **“Use option X and generate everything”**
